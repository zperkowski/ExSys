package fire
rule "Fire"
   when
     m: Measurement(isSmoke(), getTemp() > 150)
   then
     System.out.print("The fire is on!\n");
end
rule "No fire"
   when
     m: Measurement(!isSmoke(), getTemp() < 150)
   then
     System.out.print("There is no fire.\n");
end
